name: Memory Check Workflow
on: push
jobs:
  memcheck:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: sudo apt-get update
      - run: sudo apt-get install -y -q pkg-config check
      - run: make valgrind
